# Archived real-data experiments

Live ACS code: [`real_data/acs/`](../../real_data/acs/).

| Path | What |
|------|------|
| `acs/` | Older ACS plot trees, stratified/percentile suites, size-ignorability variants |
| `acs/legacy_local/` | Former `real_data/acs/{results,plots}/` (pre–`paper-results`) |
| `acs/data/` | Non-paper extracts (`acs_top25_filtered.csv`, trim2 PUMA summary) |
| `blood_pressure/` | Clinic BP study (data + early HCP runs); not used in the paper |
| `scripts/` | `repeated_experiments*.py`, early `acs_true_marginal_experiments.py`, `plot_results.py` |

Paper ACS runner: `code/marginal/run_acs_yoep_fb_min21_permute.py`.
